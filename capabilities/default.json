{
  "$schema": "https://schema.tauri.app/core-schema/2",
  "identifier": "default",
  "description": "Default permissions for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
